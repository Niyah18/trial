import 'package:flutter/material.dart';
 class HiddenProfilePage extends StatelessWidget {
   @override Widget build(BuildContext context) {
     return Scaffold(
       appBar: AppBar(
         title: Text('Hidden Profile Page'),
          backgroundColor: Colors.teal, ), 
          body: Center( 
            child: Text('This is the Hidden Profile Page.'),
             ),
              );
               }
                }